version: "3.8"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.app
      args:
        SCOPE: api
    image: bitmetro/booking-api:test5
    container_name: booking-api
    ports:
      - 3001:3001
    env_file: ./.env.docker
  web:
    build:
      context: .
      dockerfile: Dockerfile.web
      args:
        SCOPE: web
    image: bitmetro/booking-web:test5
    container_name: booking-web
    ports:
      - 3000:3000
    env_file: ./.env.docker
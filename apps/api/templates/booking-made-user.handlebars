<h3>Hello {{ user.name }}!</h3>
<p>
  Your booking has been successfully made with <a href="{{ operator.url }}">{{ operator.name }}</a> for <a href="{{ service.url }}">{{ service.name }}</a>
</p>
<p>
  You can view <a href="{{ booking.url }}">this page</a> to view the booking
</p>
